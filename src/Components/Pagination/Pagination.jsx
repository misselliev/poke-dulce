import './Pagination.css'

const Pagination = () => {

  return (
    <nav>
        <button disabled>
        Previous
        </button>
        <button disabled>
        Next
        </button>
    </nav>
  );
};

export default Pagination